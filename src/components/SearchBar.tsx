import * as React from "react";
import { Input } from "semantic-ui-react";

export const SearchBar = () => (
  <Input placeholder="Search..." iconPosition="left" icon="search" style={{ width: "80%" }} />
)
